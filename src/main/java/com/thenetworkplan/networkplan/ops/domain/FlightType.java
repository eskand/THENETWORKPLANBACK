package com.thenetworkplan.networkplan.ops.domain;

/** ICAO flight-plan item 8 intent, as far as the operator declares it. */
public enum FlightType {
    PAX,
    FERRY,
    POSITIONING,
    TRAINING,
    MAINTENANCE,
    AMBULANCE
}
