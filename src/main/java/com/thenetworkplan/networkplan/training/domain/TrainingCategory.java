package com.thenetworkplan.networkplan.training.domain;

/** Family of a course, as the training manual classifies it. */
public enum TrainingCategory {
    INITIAL,
    RECURRENT,
    CRM,
    DANGEROUS_GOODS,
    SEP,
    SIMULATOR,
    LINE_TRAINING,
    SECURITY,
    GROUND
}
