package com.thenetworkplan.networkplan.sales.domain;

/** Nature of the client, which drives the terms more than the price. */
public enum ClientKind {
    CORPORATE,
    BROKER,
    GOVERNMENT,
    PRIVATE,
    MEDICAL
}
